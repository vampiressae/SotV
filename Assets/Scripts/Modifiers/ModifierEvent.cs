namespace Modifier
{
    public enum ModifierEvent
    {
        OnAdded,
        OnRemoved,
        OnTurnStarted,
        OnTurnEnded,
    }
    public enum ModifierEventInt
    {
        OnDamageDeal,
        OnDamageReceive,
    }
}
